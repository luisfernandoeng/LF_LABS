<Window x:Name="ToastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="320" Height="80"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="False"
        ShowActivated="False"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    
    <Border Background="#E6F2FA" 
            CornerRadius="8" 
            BorderBrush="#BEDAFA" 
            BorderThickness="1"
            Padding="15,10"
            Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="#000000" Opacity="0.15" ShadowDepth="2"/>
        </Border.Effect>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Ícone -->
            <TextBlock x:Name="ToastIcon" 
                       Grid.Column="0"
                       Text="💾" 
                       FontSize="20" 
                       VerticalAlignment="Center"
                       Margin="0,0,12,0"/>
            
            <!-- Conteúdo -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock x:Name="ToastTitle" 
                           Text="AutoSave Concluído" 
                           FontWeight="SemiBold" 
                           FontSize="13"
                           Foreground="#0078D7"/>
                <TextBlock x:Name="ToastMessage" 
                           Text="Projeto salvo com sucesso." 
                           FontSize="11" 
                           Foreground="#556677"
                           Margin="0,2,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
