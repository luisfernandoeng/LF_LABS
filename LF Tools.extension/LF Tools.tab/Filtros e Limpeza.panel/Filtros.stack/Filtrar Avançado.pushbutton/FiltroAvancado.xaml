<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filtro Avançado - Luís Fernando"
        Height="820"
        Width="620"
        MinHeight="720"
        MinWidth="580"
        Background="#F2F2F2"
        Foreground="#333333"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI"
        FontSize="13">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <SolidColorBrush x:Key="AppBackground" Color="#F2F2F2"/>
        <SolidColorBrush x:Key="PanelColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubtleSurface" Color="#F8F8F8"/>
        <SolidColorBrush x:Key="BorderColor" Color="#C8C8C8"/>
        <SolidColorBrush x:Key="DividerColor" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="HoverBg" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="SelectionBg" Color="#E6F2FA"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentBlueHover" Color="#106EBE"/>
        <SolidColorBrush x:Key="AccentBluePressed" Color="#005A9E"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#666666"/>
        <SolidColorBrush x:Key="TextMuted" Color="#999999"/>
        <SolidColorBrush x:Key="TextOnAccent" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Danger" Color="#D13438"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>

        <Style TargetType="Separator">
            <Setter Property="Background" Value="{StaticResource DividerColor}"/>
            <Setter Property="Height" Value="1"/>
            <Setter Property="Margin" Value="0,14"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="IsTextSearchEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Focusable="False"
                                          ClickMode="Press"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder"
                                                Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="30"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                      Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{StaticResource TextSecondary}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="10,0,30,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Background="Transparent"
                                     Foreground="{StaticResource TextPrimary}"
                                     BorderThickness="0"
                                     Margin="10,0,30,0"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center"
                                     Focusable="True"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="260">
                                    <Border Background="{StaticResource InputColor}"
                                            BorderBrush="{StaticResource BorderColor}"
                                            BorderThickness="1"
                                            Margin="0,2,0,0"
                                            CornerRadius="4">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter Property="IsTextSearchEnabled" Value="False"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SelectionBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,4,18,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="{StaticResource TextOnAccent}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="24,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBluePressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="#E6E6E6"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#DADADA"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerActionButton" TargetType="Button" BasedOn="{StaticResource SecondaryActionButton}">
            <Setter Property="Foreground" Value="{StaticResource Danger}"/>
        </Style>

        <Style x:Key="PanelBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Filtro Avançado"
                       FontSize="26"
                       FontWeight="Light"
                       Foreground="{StaticResource TextPrimary}"
                       VerticalAlignment="Center"/>
            <Button Grid.Column="1"
                    x:Name="Button_CapturarSelecao"
                    Content="Capturar seleção"
                    Style="{StaticResource SecondaryActionButton}"
                    MinWidth="132"/>
        </Grid>

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <StackPanel>
                <Border Style="{StaticResource PanelBorder}" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Filtros salvos" Style="{StaticResource SectionHeader}"/>
                            <ComboBox Name="ComboBox_FiltrosSalvos" IsEditable="False"/>
                        </StackPanel>

                        <Button Grid.Column="2"
                                Name="Button_SalvarFiltro"
                                Content="Salvar"
                                ToolTip="Salvar filtro atual"
                                Style="{StaticResource SecondaryActionButton}"
                                Width="82"
                                VerticalAlignment="Bottom"/>

                        <Button Grid.Column="4"
                                Name="Button_DeletarFiltro"
                                Content="Excluir"
                                ToolTip="Excluir filtro salvo"
                                Style="{StaticResource DangerActionButton}"
                                Width="82"
                                VerticalAlignment="Bottom"/>
                    </Grid>
                </Border>

                <Border Style="{StaticResource PanelBorder}" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="Escopo"
                                       Style="{StaticResource SectionHeader}"
                                       Margin="0"/>
                            <Button Grid.Column="2"
                                    Name="Button_HistoricoAnterior"
                                    Content="Histórico"
                                    ToolTip="Ver histórico de seleções"
                                    Style="{StaticResource SecondaryActionButton}"
                                    Height="30"
                                    Padding="12,0"/>
                            <Button Grid.Column="4"
                                    Name="Button_LimparHistorico"
                                    Content="Limpar"
                                    ToolTip="Limpar histórico"
                                    Style="{StaticResource SecondaryActionButton}"
                                    Height="30"
                                    Padding="12,0"/>
                        </Grid>

                        <TextBlock Text="Categoria" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Categoria" IsEditable="True" Margin="0,0,0,12"/>

                        <TextBlock Text="Buscar em" Style="{StaticResource FieldLabel}"/>
                        <WrapPanel>
                            <RadioButton x:Name="Radio_VistaAtual"
                                         Content="Vista atual"
                                         GroupName="Escopo"
                                         IsChecked="True"
                                         Style="{StaticResource ModernRadioButton}"/>
                            <RadioButton x:Name="Radio_ProjetoInteiro"
                                         Content="Projeto inteiro"
                                         GroupName="Escopo"
                                         Style="{StaticResource ModernRadioButton}"/>
                            <RadioButton x:Name="Radio_SelecaoAtual"
                                         Content="Seleção atual (0 elementos)"
                                         GroupName="Escopo"
                                         Style="{StaticResource ModernRadioButton}"
                                         Visibility="Collapsed"/>
                        </WrapPanel>

                        <CheckBox x:Name="CheckBox_FamiliasAninhadas"
                                  Content="Incluir famílias aninhadas"
                                  Style="{StaticResource ModernCheckBox}"
                                  Margin="0,8,0,0"/>

                        <ComboBox x:Name="ComboBox_Escopo" Visibility="Collapsed">
                            <ComboBoxItem Content="Vista Atual" IsSelected="True"/>
                            <ComboBoxItem Content="Projeto Inteiro"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource PanelBorder}" Padding="16" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Regra 1" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Parâmetro" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Parametro1" IsEditable="True" Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="190"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Condição" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="ComboBox_Condicao1">
                                    <ComboBoxItem Content="Contém" IsSelected="True"/>
                                    <ComboBoxItem Content="Não Contém"/>
                                    <ComboBoxItem Content="Diferente de"/>
                                    <ComboBoxItem Content="Em branco"/>
                                    <ComboBoxItem Content="Não em branco"/>
                                    <ComboBoxItem Content="Maior que"/>
                                    <ComboBoxItem Content="Menor que"/>
                                    <ComboBoxItem Content="Maior ou igual"/>
                                    <ComboBoxItem Content="Menor ou igual"/>
                                    <ComboBoxItem Content="Igual a"/>
                                    <ComboBoxItem Content="Começa com"/>
                                    <ComboBoxItem Content="Termina com"/>
                                    <ComboBoxItem Content="Corresponde ao Padrão"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Valor" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TextBox_Valor1"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource PanelBorder}" Padding="16" Margin="0,0,0,12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                            <RadioButton x:Name="Radio_And"
                                         GroupName="Logic"
                                         Content="E"
                                         IsChecked="True"
                                         Style="{StaticResource ModernRadioButton}"
                                         Margin="0,0,18,0"/>
                            <RadioButton x:Name="Radio_Or"
                                         GroupName="Logic"
                                         Content="OU"
                                         Style="{StaticResource ModernRadioButton}"
                                         Margin="0"/>
                        </StackPanel>

                        <CheckBox Grid.Column="2"
                                  x:Name="CheckBox_UsarSegundoFiltro"
                                  Content="Ativar segunda regra"
                                  Style="{StaticResource ModernCheckBox}"
                                  FontWeight="SemiBold"
                                  Margin="20,0,0,0"/>
                    </Grid>
                </Border>

                <ComboBox x:Name="ComboBox_Operador" Visibility="Collapsed" SelectedIndex="0"/>

                <Border Style="{StaticResource PanelBorder}"
                        Padding="16"
                        Margin="0,0,0,12"
                        IsEnabled="{Binding ElementName=CheckBox_UsarSegundoFiltro, Path=IsChecked}"
                        Visibility="{Binding ElementName=CheckBox_UsarSegundoFiltro, Path=IsChecked, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="Regra 2" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="Parâmetro" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Parametro2" IsEditable="True" Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="190"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Condição" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="ComboBox_Condicao2">
                                    <ComboBoxItem Content="Contém" IsSelected="True"/>
                                    <ComboBoxItem Content="Não Contém"/>
                                    <ComboBoxItem Content="Diferente de"/>
                                    <ComboBoxItem Content="Em branco"/>
                                    <ComboBoxItem Content="Não em branco"/>
                                    <ComboBoxItem Content="Maior que"/>
                                    <ComboBoxItem Content="Menor que"/>
                                    <ComboBoxItem Content="Maior ou igual"/>
                                    <ComboBoxItem Content="Menor ou igual"/>
                                    <ComboBoxItem Content="Igual a"/>
                                    <ComboBoxItem Content="Começa com"/>
                                    <ComboBoxItem Content="Termina com"/>
                                    <ComboBoxItem Content="Corresponde ao Padrão"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Valor" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TextBox_Valor2"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelColor}"
                    BorderBrush="{StaticResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="10,7"
                    VerticalAlignment="Center"
                    Margin="0,0,12,0">
                <TextBlock x:Name="TextBlock_Status"
                           Text="Pronto"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondary}"
                           TextTrimming="CharacterEllipsis"/>
            </Border>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="Button_Fechar"
                        Content="Fechar"
                        Style="{StaticResource SecondaryActionButton}"
                        Width="96"
                        Margin="0,0,8,0"/>

                <Button x:Name="Button_AplicarFiltro"
                        Content="Aplicar filtro"
                        Style="{StaticResource PrimaryActionButton}"
                        Width="144"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
