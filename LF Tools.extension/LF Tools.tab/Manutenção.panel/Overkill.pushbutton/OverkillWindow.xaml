<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Overkill - Limpeza de Duplicados"
        Height="700" Width="650"
        Background="#FF111317"
        Foreground="#FFF2F4F8"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        ResizeMode="NoResize">

    <Window.Resources>
        <SolidColorBrush x:Key="PanelColor" Color="#FF171A1F"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF313844"/>
        <SolidColorBrush x:Key="InputColor" Color="#FF13161C"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF80D8C8"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#FF80D8C8"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF80D8C8"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF2F4F8"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFC4CAD4"/>
        <SolidColorBrush x:Key="Warning" Color="#FFF7C453"/>
        <SolidColorBrush x:Key="Danger" Color="#FFEF5350"/>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="StandardPanel" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
            <Setter Property="CornerRadius" Value="18"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="RadioBorder" Width="14" Height="14" BorderThickness="2" BorderBrush="{StaticResource BorderColor}" CornerRadius="7" Margin="0,0,6,0" VerticalAlignment="Center">
                                <Ellipse x:Name="RadioDot" Width="6" Height="6" Fill="{StaticResource AccentBlue}" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioDot" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                             <Border x:Name="CheckBorder" Width="16" Height="16" BorderThickness="1.5" BorderBrush="{StaticResource BorderColor}" CornerRadius="5" Margin="0,0,8,0" VerticalAlignment="Center">
                                <Path x:Name="CheckMark" Width="8" Height="8" Stretch="Uniform" Fill="White" Data="M 0,4 L 3,7 L 8,0" StrokeThickness="2" Stroke="White" Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center" Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="#103f37"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="21" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#A0E8D8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0AFFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ListBoxItem">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border Background="Transparent" Padding="2">
                            <ContentPresenter/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="OVERKILL REVIT" FontSize="20" FontWeight="Light" Foreground="{StaticResource AccentBlue}" HorizontalAlignment="Center"/>
            <TextBlock Text="Limpeza de elementos duplicados e sobrepostos" FontSize="11" Foreground="{StaticResource TextSecondary}" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- FASE 1: CONFIGURAÇÃO (mostrado inicialmente) -->
        <Grid x:Name="config_grid" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Opções compactas em linha -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="15"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ESCOPO -->
                <Border Grid.Column="0" Style="{StaticResource StandardPanel}">
                    <StackPanel>
                        <TextBlock Text="ESCOPO" Style="{StaticResource SectionHeader}"/>
                        <RadioButton x:Name="scope_view_rb" GroupName="Scope" Content="Vista Ativa" IsChecked="True" Style="{StaticResource ModernRadioButton}"/>
                        <RadioButton x:Name="scope_project_rb" GroupName="Scope" Content="Todo o Projeto" Style="{StaticResource ModernRadioButton}"/>
                    </StackPanel>
                </Border>

                <!-- MODO -->
                <Border Grid.Column="2" Style="{StaticResource StandardPanel}">
                    <StackPanel>
                        <TextBlock Text="AÇÃO" Style="{StaticResource SectionHeader}"/>
                        <RadioButton x:Name="mode_delete_rb" GroupName="Mode" Content="Deletar" IsChecked="True" Style="{StaticResource ModernRadioButton}"/>
                        <RadioButton x:Name="mode_select_rb" GroupName="Mode" Content="Apenas Selecionar" Style="{StaticResource ModernRadioButton}"/>
                    </StackPanel>
                </Border>

                <!-- TOLERÂNCIA -->
                <Border Grid.Column="4" Style="{StaticResource StandardPanel}">
                    <StackPanel>
                        <TextBlock Text="TOLERÂNCIA (MM)" Style="{StaticResource SectionHeader}"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="6"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Lin:" Style="{StaticResource FieldLabel}" Margin="0,0,6,0"/>
                            <TextBox Grid.Row="0" Grid.Column="1" x:Name="tol_curve_tb" Text="1.0"/>
                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Pt:" Style="{StaticResource FieldLabel}" Margin="0,0,6,0"/>
                            <TextBox Grid.Row="2" Grid.Column="1" x:Name="tol_point_tb" Text="2.0"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- CATEGORIAS -->
            <Border Grid.Row="1" Style="{StaticResource StandardPanel}" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="0,0,0,6">
                        <TextBlock Text="CATEGORIAS ALVO" Style="{StaticResource SectionHeader}" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="cat_all_btn" Content="Todas" Style="{StaticResource SecondaryActionButton}" Height="22" FontSize="10" Padding="8,0" Margin="0,0,4,0"/>
                            <Button x:Name="cat_none_btn" Content="Nenhuma" Style="{StaticResource SecondaryActionButton}" Height="22" FontSize="10" Padding="8,0"/>
                        </StackPanel>
                    </Grid>
                    <ListBox Grid.Row="1" x:Name="categories_lb" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Foreground="{StaticResource TextPrimary}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <CheckBox Content="{Binding name}" IsChecked="{Binding is_checked}" Style="{StaticResource ModernCheckBox}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- FASE 2: RESULTADOS (inicialmente oculto, substitui a config) -->
        <Grid x:Name="results_grid" Grid.Row="1" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Resumo -->
            <Border Grid.Row="0" Style="{StaticResource StandardPanel}" Background="#15F7C453">
                <StackPanel>
                    <TextBlock x:Name="summary_tb" Text="" FontSize="13" Foreground="{StaticResource Warning}" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Toolbar dos Resultados -->
            <Grid Grid.Row="1" Margin="0,0,0,6">
                <TextBlock Text="ELEMENTOS DUPLICADOS" Style="{StaticResource SectionHeader}" VerticalAlignment="Center"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="res_all_btn" Content="Marcar Todos" Style="{StaticResource SecondaryActionButton}" Height="22" FontSize="10" Padding="8,0" Margin="0,0,4,0"/>
                    <Button x:Name="res_none_btn" Content="Desmarcar Todos" Style="{StaticResource SecondaryActionButton}" Height="22" FontSize="10" Padding="8,0" Margin="0,0,4,0"/>
                    <Button x:Name="back_btn" Content="← Voltar" Style="{StaticResource SecondaryActionButton}" Height="22" FontSize="10" Padding="8,0"/>
                </StackPanel>
            </Grid>

            <!-- DataGrid de Elementos Individuais -->
            <Border Grid.Row="2" Style="{StaticResource StandardPanel}" Padding="0">
                <DataGrid x:Name="results_dg" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          RowBackground="Transparent"
                          AlternatingRowBackground="#11FFFFFF"
                          Foreground="{StaticResource TextPrimary}"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="{StaticResource BorderColor}"
                          IsReadOnly="False"
                          SelectionMode="Single">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource PanelColor}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="6"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="✓" Width="40">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Center" Style="{StaticResource ModernCheckBox}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="ID" Binding="{Binding ElementIdStr}" Width="70" IsReadOnly="True" Foreground="{StaticResource TextSecondary}"/>
                        <DataGridTextColumn Header="Categoria" Binding="{Binding CategoryName}" Width="140" IsReadOnly="True" Foreground="{StaticResource AccentTeal}"/>
                        <DataGridTextColumn Header="Descrição" Binding="{Binding Description}" Width="*" IsReadOnly="True" Foreground="{StaticResource TextPrimary}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- AÇÕES -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="cancel_btn" Grid.Column="0" Content="Cancelar" Style="{StaticResource SecondaryActionButton}" Width="90"/>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="run_btn" Content="ANALISAR" Style="{StaticResource PrimaryActionButton}" Background="{StaticResource AccentBlue}" Width="130" Margin="0,0,8,0"/>
                <Button x:Name="confirm_btn" Content="EXCLUIR SELECIONADOS" Style="{StaticResource PrimaryActionButton}" Background="{StaticResource Danger}" Visibility="Collapsed" Width="180"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>