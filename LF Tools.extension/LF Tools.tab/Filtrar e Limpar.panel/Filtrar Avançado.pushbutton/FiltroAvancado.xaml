<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Filtro Avançado PRO - Luís Fernando"
        Height="720" 
        Width="540"
        Background="#FF1E1E1E"
        Foreground="#FFF1F1F1"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- Paleta de Cores -->
        <SolidColorBrush x:Key="PanelColor" Color="#FF252526"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="InputColor" Color="#FF333337"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF569CD6"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#FF4EC9B0"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF4EC99B"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF1F1F1"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFCCCCCC"/>

        <!-- Estilos Base -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- TextBox Dark -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center"
                                        Margin="4,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Dark -->
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                        Grid.Column="2"
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="1">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Border"
                                              Background="{TemplateBinding Background}"
                                              BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="1"
                                              CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"
                                                    Data="M 0 0 L 4 4 L 8 0 Z"
                                                    Fill="{StaticResource TextSecondary}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                            IsHitTestVisible="False"
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="10,0,25,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                   Style="{x:Null}"
                                   Background="{TemplateBinding Background}"
                                   Foreground="White"
                                   BorderThickness="0"
                                   Margin="10,0,25,0"
                                   VerticalAlignment="Center"
                                   Focusable="True"
                                   Visibility="Hidden"
                                   IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="200">
                                    <Border x:Name="DropDownBorder"
                                          Background="{StaticResource InputColor}"
                                          BorderBrush="{StaticResource BorderColor}"
                                          BorderThickness="1"
                                          CornerRadius="3"
                                          Margin="0,2,0,0">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBoxItem">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                              Background="Transparent"
                              Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#22569CD6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RadioButton Moderno -->
        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border x:Name="RadioBorder" 
                                    Width="16" Height="16" 
                                    BorderThickness="2" 
                                    BorderBrush="{StaticResource BorderColor}"
                                    CornerRadius="8"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center">
                                <Ellipse x:Name="RadioDot" 
                                        Width="6" Height="6" 
                                        Fill="{StaticResource AccentTeal}"
                                        Visibility="Collapsed"/>
                            </Border>
                            
                            <ContentPresenter Grid.Column="1" 
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Content}"/>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioDot" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox Moderno -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            
                            <Border x:Name="CheckBorder" 
                                    Width="18" Height="18" 
                                    BorderThickness="2" 
                                    BorderBrush="{StaticResource BorderColor}"
                                    CornerRadius="3"
                                    Margin="0,0,10,0"
                                    VerticalAlignment="Center">
                                <Path x:Name="CheckMark"
                                      Width="12" Height="12"
                                      Stretch="Uniform"
                                      Fill="White"
                                      Data="M 0,4 L 3,7 L 8,0"
                                      StrokeThickness="2"
                                      Stroke="White"
                                      Visibility="Collapsed"/>
                            </Border>
                            
                            <ContentPresenter Grid.Column="1" 
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Content}"/>
                        </Grid>
                        
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                                <Setter TargetName="CheckBorder" Property="Background" Value="{StaticResource AccentTeal}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentTeal}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botões -->
        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentTeal}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="*"/>     <!-- Content -->
            <RowDefinition Height="Auto"/>  <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="FILTRO AVANÇADO PRO" 
                      FontSize="20" 
                      FontWeight="Light" 
                      Foreground="{StaticResource AccentTeal}"/>
            <TextBlock Text="Configure critérios avançados de filtragem" 
                      FontSize="11" 
                      Foreground="{StaticResource TextSecondary}"
                      Margin="0,3,0,0"/>
        </StackPanel>

        <!-- CONTENT -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Configuração Inicial -->
                <Border Background="{StaticResource PanelColor}" 
                        CornerRadius="4" 
                        Padding="18"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Configuração Inicial" Style="{StaticResource SectionHeader}"/>
                        
                        <!-- Categoria -->
                        <TextBlock Text="Categoria" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Categoria" IsEditable="True" Margin="0,0,0,15"/>
                        
                        <!-- Escopo -->
                        <TextBlock Text="Escopo de Busca" Style="{StaticResource FieldLabel}"/>
                        <RadioButton x:Name="Radio_VistaAtual" 
                                    Content="Vista Atual" 
                                    GroupName="Escopo" 
                                    IsChecked="True"
                                    Style="{StaticResource ModernRadioButton}"/>
                        <RadioButton x:Name="Radio_ProjetoInteiro" 
                                    Content="Projeto Inteiro" 
                                    GroupName="Escopo"
                                    Style="{StaticResource ModernRadioButton}"/>
                        
                        <!-- ComboBox oculto para compatibilidade -->
                        <ComboBox x:Name="ComboBox_Escopo" Visibility="Collapsed">
                            <ComboBoxItem Content="Vista Atual" IsSelected="True"/>
                            <ComboBoxItem Content="Projeto Inteiro"/>
                        </ComboBox>
                    </StackPanel>
                </Border>

                <!-- Primeiro Filtro -->
                <Border Background="{StaticResource PanelColor}" 
                        CornerRadius="4" 
                        Padding="18"
                        Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Regra de Filtragem #1" Style="{StaticResource SectionHeader}"/>
                        
                        <TextBlock Text="Parâmetro" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Parametro1" IsEditable="True" Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Condição" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="ComboBox_Condicao1">
                                    <ComboBoxItem Content="Igual a" IsSelected="True"/>
                                    <ComboBoxItem Content="Contém"/>
                                    <ComboBoxItem Content="Começa com"/>
                                    <ComboBoxItem Content="Termina com"/>
                                    <ComboBoxItem Content="Diferente de"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Valor" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TextBox_Valor1"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <!-- Operador Lógico -->
                <Border Background="{StaticResource InputColor}" 
                        BorderBrush="{StaticResource BorderColor}" 
                        BorderThickness="1" 
                        CornerRadius="20" 
                        Padding="16,8"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="Radio_And" 
                                    GroupName="Logic" 
                                    Content="E (AND)" 
                                    IsChecked="True" 
                                    Style="{StaticResource ModernRadioButton}"
                                    Margin="0,0,20,0"/>
                        <RadioButton x:Name="Radio_Or" 
                                    GroupName="Logic" 
                                    Content="OU (OR)" 
                                    Style="{StaticResource ModernRadioButton}"/>
                    </StackPanel>
                </Border>
                
                <!-- ComboBox oculto para compatibilidade -->
                <ComboBox x:Name="ComboBox_Operador" Visibility="Collapsed" SelectedIndex="0"/> 

                <!-- Segundo Filtro -->
                <CheckBox x:Name="CheckBox_UsarSegundoFiltro" 
                         Content="✓ Ativar Segundo Filtro" 
                         Style="{StaticResource ModernCheckBox}"
                         FontWeight="SemiBold"
                         Margin="0,0,0,10"/>
                
                <Border Background="{StaticResource PanelColor}" 
                        CornerRadius="4" 
                        Padding="18"
                        Margin="0,0,0,12"
                        IsEnabled="{Binding ElementName=CheckBox_UsarSegundoFiltro, Path=IsChecked}"
                        Visibility="{Binding ElementName=CheckBox_UsarSegundoFiltro, Path=IsChecked, Converter={StaticResource BoolToVis}}">
                    <StackPanel>
                        <TextBlock Text="Regra de Filtragem #2" Style="{StaticResource SectionHeader}"/>
                        
                        <TextBlock Text="Parâmetro" Style="{StaticResource FieldLabel}"/>
                        <ComboBox x:Name="ComboBox_Parametro2" IsEditable="True" Margin="0,0,0,12"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="140"/>
                                <ColumnDefinition Width="12"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Condição" Style="{StaticResource FieldLabel}"/>
                                <ComboBox x:Name="ComboBox_Condicao2">
                                    <ComboBoxItem Content="Igual a" IsSelected="True"/>
                                    <ComboBoxItem Content="Contém"/>
                                    <ComboBoxItem Content="Começa com"/>
                                    <ComboBoxItem Content="Termina com"/>
                                    <ComboBoxItem Content="Diferente de"/>
                                </ComboBox>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Valor" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TextBox_Valor2"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- FOOTER -->
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Status -->
            <Border Grid.Column="0" 
                    Background="{StaticResource PanelColor}" 
                    CornerRadius="3" 
                    Padding="10,6"
                    VerticalAlignment="Center"
                    Margin="0,0,12,0">
                <TextBlock x:Name="TextBlock_Status" 
                          Text="Pronto" 
                          FontSize="10" 
                          Foreground="{StaticResource AccentTeal}"/>
            </Border>
            
            <!-- Botões -->
            <StackPanel Grid.Column="2" 
                       Orientation="Horizontal">
                <Button x:Name="Button_Fechar" 
                       Content="Fechar" 
                       Style="{StaticResource SecondaryActionButton}" 
                       Width="90" 
                       Margin="0,0,8,0"/>
                
                <Button x:Name="Button_AplicarFiltro" 
                       Content="APLICAR FILTRO" 
                       Style="{StaticResource PrimaryActionButton}" 
                       Width="160"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>