<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gerenciar Folhas — Luís Fernando"
        Height="720" Width="1320"
        MinHeight="560" MinWidth="980"
        WindowStartupLocation="CenterScreen"
        Background="#F2F2F2"
        Foreground="#333333"
        FontFamily="Segoe UI" FontSize="13"
        ShowInTaskbar="False"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- PALETA (alinhada com To Excel) -->
        <SolidColorBrush x:Key="AppBackground"       Color="#F2F2F2"/>
        <SolidColorBrush x:Key="PanelColor"          Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputColor"          Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubtleSurface"       Color="#F8F8F8"/>
        <SolidColorBrush x:Key="BorderColor"         Color="#C8C8C8"/>
        <SolidColorBrush x:Key="DividerColor"        Color="#E0E0E0"/>
        <SolidColorBrush x:Key="HoverBg"             Color="#F0F0F0"/>
        <SolidColorBrush x:Key="SelectionBg"         Color="#E6F2FA"/>
        <SolidColorBrush x:Key="AccentBlue"          Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentBlueHover"     Color="#106EBE"/>
        <SolidColorBrush x:Key="AccentBluePressed"   Color="#005A9E"/>
        <SolidColorBrush x:Key="TextPrimary"         Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondary"       Color="#777777"/>
        <SolidColorBrush x:Key="TextMuted"           Color="#999999"/>
        <SolidColorBrush x:Key="TextOnAccent"        Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Danger"              Color="#D13438"/>

        <!-- TEXTBLOCK -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily"  Value="Segoe UI"/>
            <Setter Property="Foreground"  Value="{StaticResource TextPrimary}"/>
        </Style>

        <!-- SEPARATOR -->
        <Style TargetType="Separator">
            <Setter Property="Background"      Value="{StaticResource DividerColor}"/>
            <Setter Property="Height"          Value="1"/>
            <Setter Property="Margin"          Value="0,8,0,8"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- TEXTBOX -->
        <Style TargetType="TextBox">
            <Setter Property="Background"               Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness"          Value="1"/>
            <Setter Property="Height"                   Value="32"/>
            <Setter Property="Padding"                  Value="9,0"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- COMBOBOX (editável, com autocomplete) -->
        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height"                   Value="34"/>
            <Setter Property="Background"               Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderColor}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsEditable"               Value="True"/>
            <Setter Property="IsTextSearchEnabled"      Value="True"/>
            <Setter Property="StaysOpenOnEdit"          Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder"
                                                Background="{StaticResource InputColor}"
                                                BorderBrush="{StaticResource BorderColor}"
                                                BorderThickness="1" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="28"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{StaticResource TextSecondary}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="10,0,28,0" VerticalAlignment="Center"
                                              HorizontalAlignment="Left" Visibility="Hidden"/>
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}"
                                     Background="Transparent" Foreground="{StaticResource TextPrimary}"
                                     BorderThickness="0" Margin="10,0,28,0"
                                     VerticalAlignment="Center" VerticalContentAlignment="Center"
                                     Focusable="True" Visibility="Visible"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="260">
                                    <Border Background="{StaticResource InputColor}"
                                            BorderBrush="{StaticResource BorderColor}"
                                            BorderThickness="1" Margin="0,2,0,0" CornerRadius="4">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBox" BasedOn="{StaticResource ComboBoxStyle}"/>

        <!-- COMBOBOX ITEM -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding"                    Value="10,7"/>
            <Setter Property="Foreground"                 Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background"                 Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SelectionBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TABCONTROL (underline, sem borda ao redor das tabs) -->
        <Style TargetType="TabControl">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                            <Border Grid.Row="1" Height="1" Background="{StaticResource DividerColor}" Margin="0,0,0,0"/>
                            <Border Grid.Row="2"
                                    Background="{StaticResource PanelColor}"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1,0,1,1">
                                <ContentPresenter ContentSource="SelectedContent"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TABITEM (underline) -->
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin"  Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="3"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="tab_txt" Grid.Row="0"
                                       Text="{TemplateBinding Header}"
                                       FontSize="14" FontWeight="SemiBold"
                                       Padding="4,4,16,10"
                                       Foreground="{StaticResource TextSecondary}"/>
                            <Border x:Name="tab_bar" Grid.Row="1"
                                    Background="Transparent" CornerRadius="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="tab_bar" Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DATAGRID -->
        <Style TargetType="DataGrid">
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="RowBackground"            Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource SubtleSurface}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
            <Setter Property="VerticalGridLinesBrush"   Value="Transparent"/>
            <Setter Property="HeadersVisibility"        Value="Column"/>
            <Setter Property="CanUserAddRows"           Value="False"/>
            <Setter Property="CanUserDeleteRows"        Value="False"/>
            <Setter Property="CanUserResizeColumns"     Value="True"/>
            <Setter Property="SelectionMode"            Value="Extended"/>
            <Setter Property="SelectionUnit"            Value="FullRow"/>
            <Setter Property="GridLinesVisibility"      Value="None"/>
            <Setter Property="FontSize"                 Value="12"/>
            <Setter Property="RowHeight"                Value="46"/>
            <Setter Property="RowHeaderWidth"           Value="0"/>
            <Setter Property="AutoGenerateColumns"      Value="False"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        </Style>

        <!-- DATAGRID COLUMN HEADER -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="{StaticResource SubtleSurface}"/>
            <Setter Property="Foreground"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Padding"         Value="8,7"/>
            <Setter Property="BorderBrush"     Value="#18000000"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Grid>
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </Border>
                            <Thumb x:Name="PART_LeftHeaderGripper" Width="3"
                                   HorizontalAlignment="Left" Background="Transparent" Opacity="0" Cursor="SizeWE">
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                            <Thumb x:Name="PART_RightHeaderGripper" Width="3"
                                   HorizontalAlignment="Right" Background="Transparent" Opacity="0" Cursor="SizeWE">
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Border Background="Transparent"/>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DATAGRID CELL -->
        <Style TargetType="DataGridCell">
            <Setter Property="BorderBrush"     Value="#14000000"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Padding"         Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBg}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DATAGRID ROW -->
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBg}"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- BOTÃO TOOLBAR -->
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Padding"         Value="14,0"/>
            <Setter Property="Background"      Value="#E8E8E8"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- BOTÃO ÍCONE -->
        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Width"    Value="32"/>
            <Setter Property="Padding"  Value="0"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- BOTÃO PRIMÁRIO (azul) -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height"          Value="32"/>
            <Setter Property="Padding"         Value="24,0"/>
            <Setter Property="Background"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextOnAccent}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBluePressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Gerenciar Folhas"
                           FontSize="26" FontWeight="Light"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Gerencie parâmetros do carimbo, vistas e revisões em lote"
                           FontSize="13" Foreground="{StaticResource TextSecondary}"
                           Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Perfil" Foreground="{StaticResource TextSecondary}"
                           VerticalAlignment="Center" FontSize="12" Margin="0,0,8,0"/>
                <ComboBox x:Name="ProfileCombo" Width="200" Margin="0,0,6,0"/>
                <Button x:Name="ManageProfilesBtn" Content="•••"
                        Style="{StaticResource IconButton}" ToolTip="Gerenciar perfis"/>
            </StackPanel>
        </Grid>

        <!-- TOOLBAR -->
        <WrapPanel Grid.Row="1" Margin="0,0,0,12" VerticalAlignment="Center">
            <Button x:Name="SelectAllBtn"      Content="+ Selec. Todos"  Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="SelectNoneBtn"     Content="Limpar"           Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="SetFieldBtn"       Content="Setar Campo"      Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="ReplicateStampBtn" Content="Replicar Carimbo"  Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="ApplyProfileBtn"   Content="Aplicar Perfil"   Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="SaveViewProfileBtn" Content="Salvar Perfil"   Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="SwapViewBtn"       Content="Trocar Vista"     Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="ExportBtn"         Content="Exportar"         Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="ImportBtn"         Content="Importar"         Style="{StaticResource ToolbarButton}" Margin="0,0,5,0"/>
            <Button x:Name="RefreshBtn"        Content="Atualizar"        Style="{StaticResource ToolbarButton}" Margin="0,0,12,0"/>
            <Button x:Name="DebugBtn"          Content="Debug"            Style="{StaticResource ToolbarButton}" Margin="0,0,12,0"/>
            <Grid Width="240">
                <TextBox x:Name="SearchBox" Padding="32,0,8,0"/>
                <TextBlock Text="Pesquisar…" Foreground="{StaticResource TextMuted}"
                           VerticalAlignment="Center" Margin="10,0,0,0" IsHitTestVisible="False"/>
            </Grid>
        </WrapPanel>

        <!-- TABS -->
        <TabControl Grid.Row="2" x:Name="MainTabs">

            <!-- ══ Carimbo ══ -->
            <TabItem Header="Carimbo">
                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                    <DataGrid x:Name="CarimboGrid"/>
                </Border>
            </TabItem>

            <!-- ══ Vistas ══ -->
            <TabItem Header="Vistas">
                <Border BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                    <DataGrid x:Name="ConteudoGrid"/>
                </Border>
            </TabItem>

            <!-- ══ Revisões ══ -->
            <TabItem Header="Revisões">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Painel de gestão de revisões -->
                    <Border Grid.Row="0"
                            Background="{StaticResource SubtleSurface}"
                            BorderBrush="{StaticResource DividerColor}"
                            BorderThickness="0,0,0,1"
                            Padding="16,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Revisão do documento:"
                                       VerticalAlignment="Center"
                                       Foreground="{StaticResource TextSecondary}"
                                       FontSize="12" Margin="0,0,10,0"/>
                            <ComboBox x:Name="RevisionCombo" Width="380" Margin="0,0,10,0"/>
                            <Button x:Name="NewRevisionBtn"
                                    Content="Nova Revisão"
                                    Style="{StaticResource ToolbarButton}" Margin="0,0,6,0"/>
                            <Button x:Name="AddRevisionBtn"
                                    Content="＋ Atribuir às selecionadas"
                                    Style="{StaticResource ToolbarButton}" Margin="0,0,6,0"/>
                            <Button x:Name="RemoveRevisionBtn"
                                    Content="− Remover das selecionadas"
                                    Style="{StaticResource ToolbarButton}"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" CornerRadius="4">
                        <DataGrid x:Name="RevisoesGrid"/>
                    </Border>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- FOOTER -->
        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="StatusLabel" Text="—"
                       Foreground="{StaticResource TextSecondary}"
                       VerticalAlignment="Center" FontSize="12"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="Fechar" IsCancel="True"
                        Style="{StaticResource ToolbarButton}"
                        Width="90" Margin="0,0,8,0"/>
                <Button x:Name="ApplyBtn" Content="Aplicar"
                        Style="{StaticResource PrimaryButton}" Width="110"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
