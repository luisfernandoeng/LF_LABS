<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="To Excel - Luís Fernando"
        Height="780"
        Width="1020"
        Background="#FF111317"
        Foreground="#FFF2F4F8"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">

    <Window.Resources>
        <!-- Paleta -->
        <SolidColorBrush x:Key="PanelColor"     Color="#FF171A1F"/>
        <SolidColorBrush x:Key="BorderColor"    Color="#FF313844"/>
        <SolidColorBrush x:Key="InputColor"     Color="#FF13161C"/>
        <SolidColorBrush x:Key="AccentBlue"     Color="#FFA8C7FA"/>
        <SolidColorBrush x:Key="AccentTeal"     Color="#FF8AB4F8"/>
        <SolidColorBrush x:Key="AccentGreen"    Color="#FF6ECC8A"/>
        <SolidColorBrush x:Key="AccentOrange"   Color="#FFF7C453"/>
        <SolidColorBrush x:Key="AccentRed"      Color="#FFD25555"/>
        <SolidColorBrush x:Key="TextPrimary"    Color="#FFF2F4F8"/>
        <SolidColorBrush x:Key="TextSecondary"  Color="#FFC4CAD4"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize"   Value="10"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin"     Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize"          Value="11"/>
            <Setter Property="Foreground"        Value="{StaticResource TextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin"            Value="0,0,0,5"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background"               Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground"               Value="White"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderColor}"/>
            <Setter Property="Padding"                  Value="12,0"/>
            <Setter Property="FontSize"                 Value="12"/>
            <Setter Property="Height"                   Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1" CornerRadius="17">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          VerticalAlignment="Center" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush"    Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox -->
        <Style TargetType="ComboBox">
            <Setter Property="Height"     Value="34"/>
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{StaticResource InputColor}"
                                                BorderBrush="{StaticResource BorderColor}"
                                                BorderThickness="1" CornerRadius="17">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center"
                                                      Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextSecondary}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="10,0,25,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}"
                                     Background="Transparent" Foreground="White" BorderThickness="0"
                                     Margin="10,0,25,0" VerticalAlignment="Center" VerticalContentAlignment="Center"
                                     Focusable="True" Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200">
                                    <Border Name="DropDownBorder" Background="{StaticResource InputColor}"
                                            BorderBrush="{StaticResource BorderColor}" BorderThickness="1"
                                            CornerRadius="12" Margin="0,2,0,0">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite"          Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Height"    Value="34"/>
            <Setter Property="Padding"   Value="10,0"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#15A8C7FA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CheckBox: preenchimento sólido sem checkmark -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Margin"     Value="0,4"/>
            <Setter Property="Cursor"     Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="CheckBorder"
                                    Width="15" Height="15"
                                    BorderThickness="1.5"
                                    BorderBrush="{StaticResource BorderColor}"
                                    CornerRadius="3"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"/>
                            <ContentPresenter Grid.Column="1" VerticalAlignment="Center"
                                              Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="CheckBorder" Property="Background"  Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="CheckBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabControl -->
        <Style TargetType="TabControl">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                            <Border  Grid.Row="1" Height="1" Background="{StaticResource BorderColor}" Margin="0,0,0,18"/>
                            <ContentPresenter Grid.Row="2" ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TabItem -->
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin"  Value="0,0,5,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="3"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="tab_txt" Grid.Row="0"
                                       Text="{TemplateBinding Header}"
                                       FontSize="15" FontWeight="SemiBold"
                                       Padding="15,4,15,12"
                                       Foreground="{StaticResource TextSecondary}"/>
                            <Border x:Name="tab_bar" Grid.Row="1"
                                    Background="Transparent" CornerRadius="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="tab_bar" Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid -->
        <Style TargetType="DataGrid">
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="RowBackground"            Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="#07FFFFFF"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="VerticalGridLinesBrush"   Value="Transparent"/>
            <Setter Property="HeadersVisibility"        Value="Column"/>
            <Setter Property="CanUserAddRows"           Value="False"/>
            <Setter Property="CanUserDeleteRows"        Value="False"/>
            <Setter Property="SelectionMode"            Value="Single"/>
            <Setter Property="IsReadOnly"               Value="True"/>
            <Setter Property="FontSize"                 Value="12"/>
            <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="#FF0E1116"/>
            <Setter Property="Foreground"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Padding"         Value="10,8"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#22A8C7FA"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0AFFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Botão Primário -->
        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background"            Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground"            Value="#10243f"/>
            <Setter Property="FontWeight"            Value="SemiBold"/>
            <Setter Property="FontSize"              Value="14"/>
            <Setter Property="Height"                Value="44"/>
            <Setter Property="Cursor"                Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value></Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="22">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C2D7FF"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.15"/>
                                </Storyboard></BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.15"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.15"/>
                                </Storyboard></BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botão Secundário -->
        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"        Value="12"/>
            <Setter Property="Height"          Value="40"/>
            <Setter Property="Padding"         Value="8,0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background"  Value="#0AFFFFFF"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextSecondary}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Botão Browse -->
        <Style x:Key="BrowseButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Height"     Value="34"/>
            <Setter Property="Width"      Value="34"/>
            <Setter Property="Cursor"     Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="17">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C2D7FF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="TO EXCEL"
                       FontSize="22" FontWeight="Light"
                       Foreground="{StaticResource AccentBlue}"/>
            <TextBlock x:Name="TextBlock_Status"
                       Text="Pronto para iniciar"
                       FontSize="11" Foreground="{StaticResource TextSecondary}"
                       Margin="0,4,0,0"/>
        </StackPanel>

        <!-- TABS -->
        <TabControl Grid.Row="1" x:Name="tab_Main">

            <!-- ═══════════ ABA EXPORTAR ═══════════ -->
            <TabItem Header="EXPORTAR">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Painel de Controles -->
                    <Border Grid.Column="0"
                            Background="{StaticResource PanelColor}"
                            CornerRadius="18" Padding="24"
                            Margin="0,0,10,0">
                        <!-- O Grid usa um ClipToBounds=False implícito para o dropdown
                             flutuar sobre as linhas abaixo sem deslocar o layout. -->
                        <Grid ClipToBounds="False">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="CONFIGURAÇÕES"
                                       Style="{StaticResource SectionHeader}"/>

                            <!-- Modo -->
                            <TextBlock Grid.Row="1" Text="Modo" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Grid.Row="2" x:Name="ComboBox_ExportMode" Margin="0,0,0,14" IsEditable="True">
                                <ComboBoxItem Content="Schedule (Tabela)" IsSelected="True"/>
                                <ComboBoxItem Content="Quadro de Cargas (Painel)"/>
                            </ComboBox>

                            <!-- Tabela / seleção -->
                            <TextBlock Grid.Row="3" Text="Tabela" Style="{StaticResource FieldLabel}"/>

                            <!-- Dropdown toggle (botão de seleção) -->
                            <Border Grid.Row="4" x:Name="btn_DropdownToggle"
                                    Background="{StaticResource InputColor}"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1" CornerRadius="17"
                                    Height="34" Cursor="Hand" Margin="0,0,0,0">
                                <Grid Margin="12,0,10,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="txt_DropdownDisplay"
                                               Text="Selecionar..."
                                               VerticalAlignment="Center" FontSize="12"
                                               Foreground="{StaticResource TextPrimary}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="txt_DropdownArrow" Grid.Column="1"
                                               Text="▾" FontSize="11"
                                               VerticalAlignment="Center"
                                               Foreground="{StaticResource TextSecondary}"/>
                                </Grid>
                            </Border>

                            <!-- Checklist flutuante — mesma Row do botão, flutua sobre o conteúdo abaixo.
                                 Panel.ZIndex garante que fica na frente; Margin posiciona abaixo do botão.
                                 MaxHeight limita a altura total do painel para não cobrir área demais. -->
                            <Canvas Grid.Row="4" Panel.ZIndex="20">
                                <Border x:Name="pnl_ChecklistDropdown"
                                        Width="{Binding ActualWidth, ElementName=btn_DropdownToggle}"
                                        Canvas.Top="38" Canvas.Left="0"
                                        MaxHeight="240"
                                        Background="{StaticResource InputColor}"
                                        BorderBrush="{StaticResource AccentBlue}"
                                        BorderThickness="1" CornerRadius="12"
                                        Visibility="Collapsed">
                                    <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <!-- Selecionar todas -->
                                    <Border Grid.Row="0" Padding="12,8"
                                            BorderBrush="{StaticResource BorderColor}"
                                            BorderThickness="0,0,0,1">
                                        <CheckBox x:Name="chk_SelectAllSchedules"
                                                  Content="Selecionar todas"
                                                  Style="{StaticResource ModernCheckBox}"
                                                  Margin="0"/>
                                    </Border>
                                    <!-- Itens com scroll -->
                                    <ScrollViewer Grid.Row="1"
                                                  x:Name="sv_ScheduleChecklist"
                                                  VerticalScrollBarVisibility="Auto"
                                                  MaxHeight="190">
                                        <StackPanel x:Name="stack_ScheduleChecklist" Margin="10,6"/>
                                    </ScrollViewer>
                                </Grid>
                            </Border>
                            </Canvas>

                            <Separator Grid.Row="5" Background="{StaticResource BorderColor}" Margin="0,10,0,14"/>

                            <!-- Destino -->
                            <TextBlock Grid.Row="6" Text="Destino" Style="{StaticResource FieldLabel}"/>
                            <Grid Grid.Row="7" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TextBox_ExportPath" IsReadOnly="True" Grid.Column="0"/>
                                <Button x:Name="Button_BrowseExport" Content="📁"
                                        Grid.Column="1" Style="{StaticResource BrowseButton}" Margin="8,0,0,0"/>
                            </Grid>

                            <StackPanel Grid.Row="8" Margin="0,0,0,0">
                                <CheckBox x:Name="CheckBox_OpenFolder"
                                          Content="Abrir pasta após concluir"
                                          Style="{StaticResource ModernCheckBox}"
                                          IsChecked="True"/>

                                <CheckBox x:Name="CheckBox_KeepFormat"
                                          Content="Manter formatação do Revit"
                                          Style="{StaticResource ModernCheckBox}"
                                          Margin="0,6,0,0"/>

                                <!-- Aviso exibido só quando KeepFormat está marcado -->
                                <StackPanel x:Name="pnl_FormatWarning"
                                            Orientation="Horizontal"
                                            Margin="22,3,0,0"
                                            Visibility="Collapsed">
                                    <TextBlock Text="⚠" FontSize="10"
                                               Foreground="{StaticResource AccentOrange}"
                                               VerticalAlignment="Top" Margin="0,0,4,0"/>
                                    <TextBlock Text="Exportação visual — não pode ser reimportada ao Revit."
                                               FontSize="10"
                                               Foreground="{StaticResource AccentOrange}"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Painel direito: Preview + Botão -->
                    <Grid Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Preview -->
                        <Border Grid.Row="0"
                                Background="{StaticResource PanelColor}"
                                CornerRadius="18" Padding="22"
                                Margin="0,0,0,10">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="PRÉ-VISUALIZAÇÃO DA TABELA"
                                           Style="{StaticResource SectionHeader}"/>

                                <TextBlock Grid.Row="1" x:Name="lbl_PreviewStatus"
                                           Text="Selecione uma tabela para ver as colunas que serão exportadas."
                                           TextWrapping="Wrap" FontSize="11"
                                           Foreground="{StaticResource TextSecondary}"
                                           Margin="0,0,0,12"/>

                                <Border Grid.Row="2"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1" CornerRadius="10">
                                    <DataGrid x:Name="dg_ExportPreview"
                                              AutoGenerateColumns="True"
                                              Visibility="Collapsed"/>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- Botão Exportar -->
                        <Button Grid.Row="1" x:Name="Button_Export"
                                Content="EXPORTAR PARA EXCEL"
                                Style="{StaticResource PrimaryActionButton}"
                                MaxWidth="600" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ═══════════ ABA IMPORTAR ═══════════ -->
            <TabItem Header="IMPORTAR">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Seleção do arquivo -->
                    <Border Grid.Row="0"
                            Background="{StaticResource PanelColor}"
                            CornerRadius="18" Padding="20,16"
                            Margin="0,0,0,10">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="ARQUIVO EXCEL" Style="{StaticResource SectionHeader}" Margin="0,0,0,5"/>
                                <TextBox x:Name="TextBox_ImportPath" IsReadOnly="True"/>
                            </StackPanel>
                            <Button x:Name="Button_BrowseImport" Content="📁"
                                    Grid.Column="1" Style="{StaticResource BrowseButton}"
                                    Margin="10,0,0,0" VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>

                    <!-- Preview de alterações -->
                    <Border Grid.Row="1"
                            Background="{StaticResource PanelColor}"
                            CornerRadius="18" Padding="22"
                            Margin="0,0,0,10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="ALTERAÇÕES DETECTADAS"
                                       Style="{StaticResource SectionHeader}"/>

                            <TextBlock Grid.Row="1" x:Name="lbl_ImportStatus"
                                       Text="Selecione um arquivo Excel para ver o que será alterado no Revit."
                                       TextWrapping="Wrap" FontSize="11"
                                       Foreground="{StaticResource TextSecondary}"
                                       Margin="0,0,0,12"/>

                            <Border Grid.Row="2"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1" CornerRadius="10">
                                <DataGrid x:Name="dg_ImportPreview"
                                          AutoGenerateColumns="True"
                                          Visibility="Collapsed"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Botão Importar -->
                    <Button Grid.Row="2" x:Name="Button_Import"
                            Content="IMPORTAR DO EXCEL"
                            Style="{StaticResource PrimaryActionButton}"
                            Background="{StaticResource AccentOrange}"
                            Foreground="#10243f"
                            MaxWidth="700" HorizontalAlignment="Center" Margin="0,20,0,0"/>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- FOOTER -->
        <Grid Grid.Row="2" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="{StaticResource PanelColor}"
                    CornerRadius="15" Padding="12,8"
                    VerticalAlignment="Center" Margin="0,0,15,0">
                <StackPanel>
                    <TextBlock Text="⚡ Ferramenta criada pelo Luís Fernando"
                               FontSize="10" Foreground="{StaticResource TextSecondary}"/>
                    <TextBlock x:Name="TextBlock_Stats"
                               Text="Aguardando seleção..."
                               FontSize="9" Foreground="{StaticResource AccentTeal}"
                               Margin="0,2,0,0"/>
                </StackPanel>
            </Border>

            <Button x:Name="Button_Close" Content="Fechar"
                    Style="{StaticResource SecondaryActionButton}"
                    Grid.Column="1" Width="100"/>
        </Grid>
    </Grid>
</Window>
