<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="To Excel — Luís Fernando"
        Height="780"
        Width="1020"
        Background="#F2F2F2"
        Foreground="#333333"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display"
        FontFamily="Segoe UI" FontSize="13">

    <Window.Resources>

        <BooleanToVisibilityConverter x:Key="BoolToVis"/>

        <!-- PALETA -->
        <SolidColorBrush x:Key="AppBackground"       Color="#F2F2F2"/>
        <SolidColorBrush x:Key="PanelColor"          Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputColor"          Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SubtleSurface"       Color="#F8F8F8"/>
        <SolidColorBrush x:Key="BorderColor"         Color="#C8C8C8"/>
        <SolidColorBrush x:Key="DividerColor"        Color="#E0E0E0"/>
        <SolidColorBrush x:Key="HoverBg"             Color="#F0F0F0"/>
        <SolidColorBrush x:Key="SelectionBg"         Color="#E6F2FA"/>
        <SolidColorBrush x:Key="AccentBlue"          Color="#0078D7"/>
        <SolidColorBrush x:Key="AccentBlueHover"     Color="#106EBE"/>
        <SolidColorBrush x:Key="AccentBluePressed"   Color="#005A9E"/>
        <SolidColorBrush x:Key="AccentBlueFaint"     Color="#E6F2FA"/>
        <SolidColorBrush x:Key="TextPrimary"         Color="#333333"/>
        <SolidColorBrush x:Key="TextSecondary"       Color="#777777"/>
        <SolidColorBrush x:Key="TextMuted"           Color="#999999"/>
        <SolidColorBrush x:Key="TextOnAccent"        Color="#FFFFFF"/>
        <SolidColorBrush x:Key="Danger"              Color="#D13438"/>
        <SolidColorBrush x:Key="SuccessColor"        Color="#107C41"/>
        <SolidColorBrush x:Key="SuccessBg"           Color="#E5F3EB"/>
        <SolidColorBrush x:Key="SuccessBorder"       Color="#C4E1D0"/>
        <SolidColorBrush x:Key="ErrorBg"             Color="#FDE7E9"/>
        <SolidColorBrush x:Key="ErrorBorder"         Color="#F6C3C6"/>
        <SolidColorBrush x:Key="InfoBg"              Color="#E6F2FA"/>
        <SolidColorBrush x:Key="InfoBorder"          Color="#BEDAFA"/>
        <SolidColorBrush x:Key="AccentOrange"        Color="#C45000"/>

        <!-- TEXTBLOCK -->
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily"  Value="Segoe UI"/>
            <Setter Property="Foreground"  Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Margin"     Value="0,0,0,10"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="Margin"     Value="0,0,0,4"/>
        </Style>

        <Style x:Key="HintText" TargetType="TextBlock">
            <Setter Property="FontSize"   Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
            <Setter Property="Margin"     Value="0,3,0,0"/>
        </Style>

        <!-- SEPARATOR -->
        <Style TargetType="Separator">
            <Setter Property="Background"      Value="{StaticResource DividerColor}"/>
            <Setter Property="Height"          Value="1"/>
            <Setter Property="Margin"          Value="0,12,0,12"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- TEXTBOX -->
        <Style TargetType="TextBox">
            <Setter Property="Background"               Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness"          Value="1"/>
            <Setter Property="Height"                   Value="36"/>
            <Setter Property="Padding"                  Value="10,0"/>
            <Setter Property="FontSize"                 Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="4,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- COMBOBOX (searchable) -->
        <Style TargetType="ComboBox">
            <Setter Property="Height"                   Value="36"/>
            <Setter Property="Background"               Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderBrush"              Value="{StaticResource BorderColor}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="IsEditable"               Value="True"/>
            <Setter Property="IsTextSearchEnabled"      Value="False"/>
            <Setter Property="StaysOpenOnEdit"          Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="ComboBorder"
                                                Background="{StaticResource InputColor}"
                                                BorderBrush="{StaticResource BorderColor}"
                                                BorderThickness="1" CornerRadius="4">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="28"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{StaticResource TextSecondary}"
                                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource TextMuted}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="ComboBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="10,0,28,0" VerticalAlignment="Center"
                                              HorizontalAlignment="Left" Visibility="Hidden"/>
                            <TextBox x:Name="PART_EditableTextBox" Style="{x:Null}"
                                     Background="Transparent" Foreground="{StaticResource TextPrimary}"
                                     BorderThickness="0" Margin="10,0,28,0"
                                     VerticalAlignment="Center" VerticalContentAlignment="Center"
                                     Focusable="True" Visibility="Visible"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup x:Name="Popup" Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid MinWidth="{TemplateBinding ActualWidth}" MaxHeight="260">
                                    <Border Background="{StaticResource InputColor}"
                                            BorderBrush="{StaticResource BorderColor}"
                                            BorderThickness="1" Margin="0,2,0,0" CornerRadius="4">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- COMBOBOX ITEM -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Padding"                    Value="10,8"/>
            <Setter Property="Foreground"                 Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background"                 Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource HoverBg}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource SelectionBg}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- CHECKBOX -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"   Value="13"/>
            <Setter Property="Margin"     Value="0,4"/>
            <Setter Property="Cursor"     Value="Hand"/>
        </Style>

        <!-- TABCONTROL -->
        <Style TargetType="TabControl">
            <Setter Property="Background"      Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                            <Border Grid.Row="1" Height="1" Background="{StaticResource DividerColor}" Margin="0,0,0,18"/>
                            <ContentPresenter Grid.Row="2" ContentSource="SelectedContent"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- TABITEM (underline) -->
        <Style TargetType="TabItem">
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin"  Value="0,0,8,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="3"/>
                            </Grid.RowDefinitions>
                            <TextBlock x:Name="tab_txt" Grid.Row="0"
                                       Text="{TemplateBinding Header}"
                                       FontSize="14" FontWeight="SemiBold"
                                       Padding="4,4,16,10"
                                       Foreground="{StaticResource TextSecondary}"/>
                            <Border x:Name="tab_bar" Grid.Row="1"
                                    Background="Transparent" CornerRadius="2"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource AccentBlue}"/>
                                <Setter TargetName="tab_bar" Property="Background" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="tab_txt" Property="Foreground" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DATAGRID -->
        <Style TargetType="DataGrid">
            <Setter Property="Background"               Value="Transparent"/>
            <Setter Property="Foreground"               Value="{StaticResource TextPrimary}"/>
            <Setter Property="BorderThickness"          Value="0"/>
            <Setter Property="RowBackground"            Value="Transparent"/>
            <Setter Property="AlternatingRowBackground" Value="{StaticResource SubtleSurface}"/>
            <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource DividerColor}"/>
            <Setter Property="VerticalGridLinesBrush"   Value="Transparent"/>
            <Setter Property="HeadersVisibility"        Value="Column"/>
            <Setter Property="CanUserAddRows"           Value="False"/>
            <Setter Property="CanUserDeleteRows"        Value="False"/>
            <Setter Property="SelectionMode"            Value="Single"/>
            <Setter Property="IsReadOnly"               Value="True"/>
            <Setter Property="FontSize"                 Value="12"/>
            <Setter Property="GridLinesVisibility"      Value="Horizontal"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background"      Value="{StaticResource SubtleSurface}"/>
            <Setter Property="Foreground"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="FontWeight"      Value="SemiBold"/>
            <Setter Property="FontSize"        Value="11"/>
            <Setter Property="Padding"         Value="10,8"/>
            <Setter Property="BorderBrush"     Value="{StaticResource DividerColor}"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridColumnHeader">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding"         Value="10,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource SelectionBg}"/>
                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverBg}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- BOTÃO PRIMÁRIO (mantém animação de escala) -->
        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background"            Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground"            Value="{StaticResource TextOnAccent}"/>
            <Setter Property="FontWeight"            Value="SemiBold"/>
            <Setter Property="FontSize"              Value="13"/>
            <Setter Property="Height"                Value="36"/>
            <Setter Property="Padding"               Value="28,0"/>
            <Setter Property="BorderThickness"       Value="0"/>
            <Setter Property="Cursor"                Value="Hand"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value><ScaleTransform ScaleX="1" ScaleY="1"/></Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBluePressed}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.02" Duration="0:0:0.12"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.02" Duration="0:0:0.12"/>
                                </Storyboard></BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard><Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.12"/>
                                    <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.12"/>
                                </Storyboard></BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- BOTÃO SECUNDÁRIO -->
        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background"      Value="#E0E0E0"/>
            <Setter Property="Foreground"      Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize"        Value="13"/>
            <Setter Property="Height"          Value="36"/>
            <Setter Property="Padding"         Value="16,0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D0D0D0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#C4C4C4"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- BOTÃO BROWSE -->
        <Style x:Key="BrowseButton" TargetType="Button">
            <Setter Property="Background"      Value="{StaticResource AccentBlue}"/>
            <Setter Property="Foreground"      Value="{StaticResource TextOnAccent}"/>
            <Setter Property="FontSize"        Value="14"/>
            <Setter Property="Height"          Value="36"/>
            <Setter Property="Width"           Value="36"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor"          Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBlueHover}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBluePressed}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PANEL BORDER -->
        <Style x:Key="PanelBorder" TargetType="Border">
            <Setter Property="Background"      Value="{StaticResource PanelColor}"/>
            <Setter Property="BorderBrush"     Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius"    Value="6"/>
        </Style>

    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="To Excel"
                           FontSize="26" FontWeight="Light"
                           Foreground="{StaticResource TextPrimary}"/>
                <TextBlock x:Name="TextBlock_Status"
                           Text="Pronto para iniciar"
                           FontSize="13" Foreground="{StaticResource TextSecondary}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Grid>

        <!-- TABS -->
        <TabControl Grid.Row="1" x:Name="tab_Main">

            <!-- ═══════════ ABA EXPORTAR ═══════════ -->
            <TabItem Header="Exportar">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Painel de Controles -->
                    <Border Grid.Column="0" Style="{StaticResource PanelBorder}" Padding="20">
                        <Grid ClipToBounds="False">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Configurações"
                                       Style="{StaticResource SectionHeader}"/>

                            <!-- Modo -->
                            <TextBlock Grid.Row="1" Text="Modo" Style="{StaticResource FieldLabel}"/>
                            <ComboBox Grid.Row="2" x:Name="ComboBox_ExportMode" Margin="0,0,0,14">
                                <ComboBoxItem Content="Schedule (Tabela)" IsSelected="True"/>
                                <ComboBoxItem Content="Quadro de Cargas (Painel)"/>
                            </ComboBox>

                            <!-- Tabela / seleção -->
                            <TextBlock Grid.Row="3" Text="Tabela" Style="{StaticResource FieldLabel}"/>

                            <!-- Dropdown toggle -->
                            <Border Grid.Row="4" x:Name="btn_DropdownToggle"
                                    Background="{StaticResource InputColor}"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1" CornerRadius="4"
                                    Height="36" Cursor="Hand">
                                <Grid Margin="10,0,10,0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock x:Name="txt_DropdownDisplay"
                                               Text="Selecionar..."
                                               VerticalAlignment="Center" FontSize="13"
                                               Foreground="{StaticResource TextPrimary}"
                                               TextTrimming="CharacterEllipsis"/>
                                    <TextBlock x:Name="txt_DropdownArrow" Grid.Column="1"
                                               Text="▾" FontSize="11"
                                               VerticalAlignment="Center" HorizontalAlignment="Center"
                                               Foreground="{StaticResource TextSecondary}"/>
                                </Grid>
                            </Border>

                            <!-- Checklist flutuante -->
                            <Canvas Grid.Row="4" Panel.ZIndex="20">
                                <Border x:Name="pnl_ChecklistDropdown"
                                        Width="{Binding ActualWidth, ElementName=btn_DropdownToggle}"
                                        Canvas.Top="40" Canvas.Left="0"
                                        MaxHeight="240"
                                        Background="{StaticResource PanelColor}"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1" CornerRadius="6"
                                        Visibility="Collapsed">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <Border Grid.Row="0" Padding="12,8"
                                                BorderBrush="{StaticResource DividerColor}"
                                                BorderThickness="0,0,0,1">
                                            <CheckBox x:Name="chk_SelectAllSchedules"
                                                      Content="Selecionar todas"
                                                      Style="{StaticResource ModernCheckBox}"
                                                      Margin="0"/>
                                        </Border>
                                        <ScrollViewer Grid.Row="1"
                                                      x:Name="sv_ScheduleChecklist"
                                                      VerticalScrollBarVisibility="Auto"
                                                      MaxHeight="190">
                                            <StackPanel x:Name="stack_ScheduleChecklist" Margin="10,6"/>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </Canvas>

                            <Separator Grid.Row="5"/>

                            <!-- Destino -->
                            <TextBlock Grid.Row="6" Text="Destino" Style="{StaticResource FieldLabel}"/>
                            <Grid Grid.Row="7" Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="8"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="TextBox_ExportPath" Grid.Column="0"
                                         Background="{StaticResource SubtleSurface}"
                                         IsReadOnly="True"/>
                                <Button x:Name="Button_BrowseExport" Content="📁"
                                        Grid.Column="2" Style="{StaticResource BrowseButton}"/>
                            </Grid>

                            <StackPanel Grid.Row="8">
                                <CheckBox x:Name="CheckBox_OpenFolder"
                                          Content="Abrir pasta após concluir"
                                          Style="{StaticResource ModernCheckBox}"
                                          IsChecked="True"/>
                                <CheckBox x:Name="CheckBox_KeepFormat"
                                          Content="Manter formatação do Revit"
                                          Style="{StaticResource ModernCheckBox}"
                                          Margin="0,6,0,0"/>
                                <StackPanel x:Name="pnl_FormatWarning"
                                            Orientation="Horizontal"
                                            Margin="24,4,0,0"
                                            Visibility="Collapsed">
                                    <TextBlock Text="⚠" FontSize="10"
                                               Foreground="#C45000"
                                               VerticalAlignment="Top" Margin="0,0,4,0"/>
                                    <TextBlock Text="Exportação visual — não pode ser reimportada ao Revit."
                                               FontSize="11" Foreground="#C45000"
                                               TextWrapping="Wrap"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Painel direito: Preview + Botão -->
                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Preview -->
                        <Border Grid.Row="0" Style="{StaticResource PanelBorder}" Padding="20">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Grid Grid.Row="0" Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="Pré-visualização" Style="{StaticResource SectionHeader}" Margin="0"/>
                                    <ComboBox Grid.Column="1" x:Name="cmb_ExportPreviewSelect" 
                                              Width="150" Height="24" FontSize="11"
                                              Visibility="Collapsed"/>
                                </Grid>

                                <TextBlock Grid.Row="1" x:Name="lbl_PreviewStatus"
                                           Text="Selecione uma tabela para ver a pré-visualização dos dados."
                                           TextWrapping="Wrap"
                                           Style="{StaticResource HintText}"
                                           Margin="0,0,0,12"/>

                                <Border Grid.Row="2"
                                        BorderBrush="{StaticResource BorderColor}"
                                        BorderThickness="1" CornerRadius="4">
                                    <DataGrid x:Name="dg_ExportPreview"
                                              AutoGenerateColumns="True"
                                              Visibility="Collapsed"/>
                                </Border>
                            </Grid>
                        </Border>

                        <!-- Botão Exportar -->
                        <Button Grid.Row="2" x:Name="Button_Export"
                                Content="Exportar para Excel"
                                Style="{StaticResource PrimaryActionButton}"
                                MaxWidth="600" HorizontalAlignment="Center"/>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- ═══════════ ABA IMPORTAR ═══════════ -->
            <TabItem Header="Importar">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="12"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Seleção do arquivo -->
                    <Border Grid.Row="0" Style="{StaticResource PanelBorder}" Padding="20,16">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="8"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Arquivo Excel" Style="{StaticResource FieldLabel}"/>
                                <TextBox x:Name="TextBox_ImportPath"
                                         Background="{StaticResource SubtleSurface}"
                                         IsReadOnly="True"/>
                            </StackPanel>
                            <Button x:Name="Button_BrowseImport" Content="📁"
                                    Grid.Column="2" Style="{StaticResource BrowseButton}"
                                    VerticalAlignment="Bottom"/>
                        </Grid>
                    </Border>

                    <!-- Preview de alterações -->
                    <Border Grid.Row="2" Style="{StaticResource PanelBorder}" Padding="20">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Grid Grid.Row="0" Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Alterações detectadas" Style="{StaticResource SectionHeader}" Margin="0"/>
                                <ComboBox Grid.Column="1" x:Name="cmb_ImportPreviewSelect" 
                                          Width="150" Height="24" FontSize="11"
                                          Visibility="Collapsed"/>
                            </Grid>

                            <TextBlock Grid.Row="1" x:Name="lbl_ImportStatus"
                                       Text="Selecione um arquivo Excel para ver o que será alterado no Revit."
                                       TextWrapping="Wrap"
                                       Style="{StaticResource HintText}"
                                       Margin="0,0,0,12"/>

                            <Border Grid.Row="2"
                                    BorderBrush="{StaticResource BorderColor}"
                                    BorderThickness="1" CornerRadius="4">
                                <DataGrid x:Name="dg_ImportPreview"
                                          AutoGenerateColumns="True"
                                          Visibility="Collapsed"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Botão Importar -->
                    <Button Grid.Row="4" x:Name="Button_Import"
                            Content="Importar do Excel"
                            Style="{StaticResource PrimaryActionButton}"
                            Background="{StaticResource AccentOrange}"
                            MaxWidth="700" HorizontalAlignment="Center"/>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- FOOTER -->
        <Grid Grid.Row="2" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock x:Name="TextBlock_Stats"
                           Text="Aguardando seleção..."
                           FontSize="12"
                           Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>

            <Button x:Name="Button_Close" Content="Fechar"
                    Style="{StaticResource SecondaryActionButton}"
                    Grid.Column="1" Width="100"/>
        </Grid>
    </Grid>
</Window>
