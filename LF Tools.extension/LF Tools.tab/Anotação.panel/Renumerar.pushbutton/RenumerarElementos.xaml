<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Renumerador PRO - Luís Fernando" 
        Height="720" 
        Width="520"
        Background="#FF1E1E1E"
        Foreground="#FFF1F1F1"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        UseLayoutRounding="True"
        TextOptions.TextFormattingMode="Display">
    
    <Window.Resources>
        <SolidColorBrush x:Key="PanelColor" Color="#FF252526"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="InputColor" Color="#FF333337"/>
        <SolidColorBrush x:Key="AccentBlue" Color="#FF569CD6"/>
        <SolidColorBrush x:Key="AccentTeal" Color="#FF4EC9B0"/>
        <SolidColorBrush x:Key="AccentGreen" Color="#FF4EC99B"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#FFF1F1F1"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#FFCCCCCC"/>
        <SolidColorBrush x:Key="Warning" Color="#FFD7BA7D"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        </Style>

        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource AccentBlue}"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        VerticalAlignment="Center"
                                        Margin="5,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="{StaticResource InputColor}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                          Grid.Column="2" Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{StaticResource InputColor}" 
                                                BorderBrush="{StaticResource BorderColor}" 
                                                BorderThickness="1" CornerRadius="3">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="20"/>
                                                </Grid.ColumnDefinitions>
                                                <Path Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextSecondary}"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              Margin="8,0,25,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                            
                            <!-- TextBox para edição (IsEditable=True) -->
                            <TextBox x:Name="PART_EditableTextBox"
                                     Style="{x:Null}"
                                     Background="Transparent"
                                     Foreground="White"
                                     BorderThickness="0"
                                     Margin="8,0,25,0"
                                     VerticalAlignment="Center"
                                     VerticalContentAlignment="Center" 
                                     Focusable="True"
                                     Visibility="Hidden"
                                     IsReadOnly="{TemplateBinding IsReadOnly}"/>

                            <Popup Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" 
                                   AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                                <Grid Name="DropDown" SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="200">
                                    <Border Name="DropDownBorder" Background="{StaticResource InputColor}" 
                                            BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Margin="0,2,0,0">
                                        <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                            <StackPanel IsItemsHost="True"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style TargetType="ComboBoxItem">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="8,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border"
                              Background="Transparent"
                              Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#22569CD6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernRadioButton" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,4"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border x:Name="RadioBorder" 
                                    Width="16" Height="16" 
                                    BorderThickness="2" 
                                    BorderBrush="{StaticResource BorderColor}"
                                    CornerRadius="8"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center">
                                <Ellipse x:Name="RadioDot" 
                                        Width="6" Height="6" 
                                        Fill="{StaticResource AccentBlue}"
                                        Visibility="Collapsed"/>
                            </Border>
                            <ContentPresenter Grid.Column="1" 
                                            VerticalAlignment="Center"
                                            Content="{TemplateBinding Content}"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="RadioDot" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="RadioBorder" Property="BorderBrush" Value="{StaticResource AccentBlue}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentGreen}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="3"
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#33FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="RENUMERADOR PRO" 
                          FontSize="20" 
                          FontWeight="Light" 
                          Foreground="{StaticResource AccentTeal}"/>
                <TextBlock x:Name="TextBlock_InfoSelecao" 
                          Text="0 elementos selecionados" 
                          FontSize="10" 
                          Foreground="{StaticResource TextSecondary}"
                          Margin="0,3,0,0"/>
            </StackPanel>

            <Button x:Name="Button_Selecionar" 
                    Grid.Column="1" 
                    Content="Selecionar" 
                    Width="100" 
                    Height="32"
                    Style="{StaticResource SecondaryActionButton}"
                    VerticalAlignment="Top"/>
        </Grid>

        <Border Grid.Row="1" 
                Background="{StaticResource PanelColor}" 
                CornerRadius="4" 
                Padding="18">
            <StackPanel>
                <TextBlock Text="Método de Ordenação" Style="{StaticResource SectionHeader}"/>
                
                <RadioButton x:Name="Radio_ManterOrdem" 
                            Content="Manter ordem atual da seleção" 
                            GroupName="Metodo" 
                            IsChecked="True" 
                            Style="{StaticResource ModernRadioButton}"/>
                
                <RadioButton x:Name="Radio_Reordenar" 
                            Content="Reordenar por critério:" 
                            GroupName="Metodo" 
                            Style="{StaticResource ModernRadioButton}"/>
                
                <ComboBox x:Name="ComboBox_Criterio" 
                         Margin="24,4,0,0" 
                         IsEnabled="False">
                    <ComboBoxItem Content="ID do Elemento" IsSelected="True"/>
                    <ComboBoxItem Content="Nome (A-Z)"/>
                    <ComboBoxItem Content="Nível"/>
                </ComboBox>

                <Separator Background="{StaticResource BorderColor}" Margin="0,15"/>

                <TextBlock Text="Configurações de Numeração" Style="{StaticResource SectionHeader}"/>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Prefixo:" Style="{StaticResource FieldLabel}"/>
                    <TextBox Grid.Row="0" Grid.Column="1" x:Name="TextBox_Prefixo" Text="EL-" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Número Inicial:" Style="{StaticResource FieldLabel}"/>
                    <TextBox Grid.Row="1" Grid.Column="1" x:Name="TextBox_NumeroInicial" Text="1" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Incremento:" Style="{StaticResource FieldLabel}"/>
                    <TextBox Grid.Row="2" Grid.Column="1" x:Name="TextBox_Incremento" Text="1" Margin="0,0,0,8"/>
                    
                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Zeros (dígitos):" Style="{StaticResource FieldLabel}"/>
                    <Grid Grid.Row="3" Grid.Column="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="70"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TextBox_Digitos" Text="2" Grid.Column="0"/>
                        <TextBlock Text="Ex: 2 = 01, 02..." 
                                  Grid.Column="1"
                                  FontSize="10"
                                  Foreground="{StaticResource TextSecondary}" 
                                  VerticalAlignment="Center"
                                  Margin="10,0,0,0"/>
                    </Grid>
                </Grid>

                <Separator Background="{StaticResource BorderColor}" Margin="0,15"/>

                <TextBlock Text="Parâmetro de Destino" Style="{StaticResource SectionHeader}"/>
                <ComboBox x:Name="ComboBox_Parametro" 
                         Margin="0,0,0,12"
                         IsEditable="True" 
                         IsReadOnly="False"
                         VerticalContentAlignment="Center"/>

                <Border Background="{StaticResource InputColor}" 
                        CornerRadius="3" 
                        Padding="12"
                        Margin="0,4,0,0">
                    <StackPanel>
                        <TextBlock Text="PRÉVIA" FontSize="9" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,6"/>
                        <TextBlock x:Name="TextBlock_Previa" 
                                  Text="0 elementos | De EL-01 até EL-01" 
                                  FontWeight="SemiBold" 
                                  Foreground="{StaticResource AccentTeal}" 
                                  FontSize="11"
                                  TextWrapping="Wrap"
                                  Margin="0,0,0,8"/>
                        <TextBlock x:Name="TextBlock_PreviewExample" 
                                  Text="EL-01, EL-02, EL-03..." 
                                  FontFamily="Consolas, Courier New"
                                  FontSize="10" 
                                  Foreground="{StaticResource Warning}"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Background="{StaticResource PanelColor}" CornerRadius="3" Padding="10,6" VerticalAlignment="Center">
                <TextBlock x:Name="TextBlock_Status" Text="Pronto" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource AccentGreen}"/>
            </Border>
            
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="Button_Fechar" Content="Cancelar" Style="{StaticResource SecondaryActionButton}" Width="90" Margin="0,0,8,0"/>
                <Button x:Name="Button_Aplicar" Content="RENUMERAR" Style="{StaticResource PrimaryActionButton}" Width="140"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>